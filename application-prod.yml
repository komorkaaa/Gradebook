spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/gradebook
    username: gbuser
    password: gbpass
  data:
    redis:
      host: redis
      port: 6379
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
